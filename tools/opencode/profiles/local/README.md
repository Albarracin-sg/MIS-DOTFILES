# local

Machine-specific OpenCode overrides.

Put here:
- local filesystem paths
- machine-only MCP servers
- temporary overrides
- local experiments

Do not put shared configuration here if it should travel to another machine.
